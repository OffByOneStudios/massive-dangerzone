# Normally this would be somewhere else on your system, shareable between multiple systems.
# For this example, we have placed it in the same folder.

from madz.user_config import *

config = UserConfig([
    OptionSystemSkipDependencies(True)
])
